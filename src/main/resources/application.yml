# ????
server:
  port: 8080
  servlet:
    session:
      timeout: 21600

# ??mysql???
spring:
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://172.17.0.5:3306/flashsale?serverTimezone=Asia/Shanghai&allowMultiQueries=true
    username: root
    password: Hua4222433
    hikari:
      minimum-idle: 5
      # ?????????????600000?10???
      idle-timeout: 180000
      # ????????????10
      maximum-pool-size: 10
      # ?????????????????????,????true
      auto-commit: true
      # ?????
      pool-name: MyHikariCP
      # ??????????????????0???????????1800000?30??
      max-lifetime: 1800000
      # ?????????,??30???30000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  # redis??
  redis:
    host: 172.17.0.3
    port: 6379
    password: Hua4222433
  rabbitmq:
    host: 172.17.0.4
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        retry:
          enabled: true
          max-attempts: 3 #最大重试次数
          initial-interval: 3000 #重试间隔时间（单位毫秒）